# Backups created by install.sh
*.backup-*

# OS / editor cruft
.DS_Store
*.swp
*~

# Never commit secrets — keep these out even if added by mistake
*.pem
*.key
id_rsa*
id_ed25519*
*token*
*secret*
.env
